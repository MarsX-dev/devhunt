export type IProduct = {
    logo: string
    name: string
    title: string
    tags: string[]
    votes: number
    slug: string
}

export type IProductResult = {
    name: string
    title: string
    logo: string
    slug: string
}